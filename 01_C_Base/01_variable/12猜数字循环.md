//猜数游戏  循环 
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main(){
	srand(time(0));
	int number = rand()%100+1;//rand()就得到一个随机的整数 
	//x%n的结果是[0,n-1]的一个整数 
	int count =  0;
	int a =0;
	printf("我已经想好了一个1到100之间的数。");
	do{
		printf("请猜1到100之间数：");
		scanf("%d",&a);
		count ++;
		if(a>number){
			printf("大了.");
		}else if(a<number){
			printf("小了。");
		}
	}	while(a !=number);
	printf("太好了，你用了%d次就猜到了答案。\n",count);
	
	
	
	return 0;
} 
