//整数的分解 整数求逆 
/*一个整数是由1至多位数字组成的，如何分解出整数的各个位上的数字，然后加以计算·对一个整数做%I0的操作，就得到它的个位数;对一个整数做/0的操作，就去掉了它的个位数;.
然后再对2的结果做%10，就得到原来数的十位数了
依此类推。*/
#include <stdio.h>
int main(){
	
	//700 会输出7 ，换个输出方式就可以输出007；
	 
	int x;		//要逆序的数字 
//	scanf ("%d",&x);
	x = 700;	//我们用700测试 
	int digit;	//保存每一位的数字  
	int ret =0;	//最终逆序后的结果 
	
/*	while(x>0){
	
		digit = x%10;    //取个位 
//		printf("%d",digit);		//直接打印个位 
		ret = ret*10 +digit;
		printf("x = %d,digit=%d,ret=%d\n",x,digit,ret);
		x /=10;   //去掉个位 
	}
	printf ("%d",ret);*/
	
//分行	输出007；
		while(x>0){
	
		digit = x%10;
		printf("%d",digit);
		ret = ret*10 +digit;
//		printf("x = %d,digit=%d,ret=%d\n",x,digit,ret);
		x /=10;
	}
//	printf ("%d",ret);

/*	%10 取个位
	/10 删个位
	循环执行 → 数字逆序
	直接打印每一位 → 能输出 007
	存到变量再打印 → 只会输出 7*/
	
	
	return 0;
} 
