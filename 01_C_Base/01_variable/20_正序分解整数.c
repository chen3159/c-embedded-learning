//整数分解 
	//正序分解整数
#include <stdio.h>
int main(){
	int x;
	
	scanf("%d",&x);
	
	//x = 70000;
	
/*	两次逆序得到正序，
	int t=0;
	do{				  
		int d = x%10;
		t = t*10+d;
		x /10;
	}while ( x>0 );
	printf("x=%d,t=%d\n", x, t);
	x = t;
	do{
		int d = x % 10;
		printf("%d",d);
		if ( x > 9 ){
			printf (" ");
		}
		x /=10;
	}	while( x > 0);*/

/*
13425 / 10000 -> 1
13425 % 10000 -> 3425
10000 / 10 -> 1000
3425 / 1000 ->3
3425 % 1000 -> 425
1000 / 10 -> 100
425 / 100 ->4
425 % 100 ->25
100 / 10 ->10
25 / 10 ->2
25 % 10 ->5
10 / 10 ->1
5 / 1 -> 5
5 % 1 ->5
1 / 10 -> 0

*/
	//正序分解
// --- 第一部分：计算mask（找“尺子”） ---
    int mask = 1;    // 初始化mask为1，这是我们的“尺子”，用来定位最高位
    int t = x;       // 把x的值复制给临时变量t，避免后面计算把原数x改坏
    while (t > 9) {  // 循环条件：只要t还是两位数及以上，就继续循环
        t /= 10;      // 砍掉t的最后一位（比如13425 → 1342）
        mask *= 10;   // mask放大10倍（比如1 → 10 → 100...）
    }
    // 循环结束后：mask就是“最高位的除数”，比如5位数→10000，3位数→100
    printf("x=%d, mask=%d\n", x, mask); // 打印原数x和算好的mask，方便调试

    // --- 第二部分：从左到右逐位打印 ---
    do {
        int d = x / mask;  // 用“尺子”mask量出当前最高位（比如13425/10000=1）
        printf("%d", d);   // 把这一位数字打印出来
        if (mask > 9) {    // 判断：如果mask>9（说明不是最后一位）
            printf(" ");   // 就在数字后面加一个空格，方便分隔
        }
        x %= mask;         // 去掉已经打印过的最高位（比如13425%10000=3425）
        mask /= 10;        // 尺子缩小10倍，准备量下一位（比如10000→1000）
    } while (mask > 0);    // 循环条件：只要mask还没变成0，就继续打印
    printf("\n");          // 所有位打印完后，换行
	
	return 0;
} 
